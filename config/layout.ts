import { Layout } from "@/types"

/* ====================
[> LAYOUT OPTIONS <]
-- left, right, center
 ==================== */

export const layout: Layout = {
  heroHeader: `left`,
  headers: {
    featureCards: `center`,
  },
}
